// Realizado por: David Rivadeneira

let Numero = prompt("Escriba el numero a verificar")

if (Numero % 2 == 0) {
    alert("El numero es divisible por 2");
} else {
    alert("no es divisible por 2");
}
